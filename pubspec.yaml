name: flutter_application_10
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.4
  firebase_core: ^3.9.0
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  assets:
    - assets/images/blue_ball.webp
    - assets/images/braun_ball.webp
    - assets/images/grey_ball.webp
    - assets/images/Jersey_brooklyn.webp
    - assets/images/jogel_ball.webp
    - assets/images/light_ball.webp
    - assets/images/molten_ball.webp
    - assets/images/orange_ball.webp
    - assets/images/purple_ball.webp
    - assets/images/spalding_ball.webp
    - assets/images/VTB_ball.webp
    - assets/images/default_product.jpg

  uses-material-design: true

# Добавьте этот блок для настройки иконки
flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon_project.png"